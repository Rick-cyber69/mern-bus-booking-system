# ==============================================================================
# VeloxBus Backend Environment Configuration
# ==============================================================================

# Server Port (Render automatically supplies PORT=10000)
PORT=5000

# Environment Mode: 'development' or 'production'
NODE_ENV=production

# MongoDB Atlas Cloud Database Connection String
MONGO_URI=mongodb+srv://deysandipan326_db_user:GJiksO5rJDf0IMkW@veloxbus-db.dqldilm.mongodb.net/bus_booking_db?retryWrites=true&w=majority&appName=veloxbus-db

# Redis Concurrency & Locking Engine (Optional - uses in-memory fallback if not provided)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Authentication Secrets
JWT_SECRET=super_secret_production_jwt_key_2026_antigravity
JWT_EXPIRE=30d

# Allowed Frontend CORS Origin
CLIENT_URL=http://localhost:3000
